.PHONY: install

install:
	cp ackrc ~/.ackrc
	cp aliases ~/.aliases
	cp emacs ~/.emacs
	cp foprc ~/.foprc
	cp gitrc ~/.gitrc
	cp gitignore_global ~/.gitignore_global
	cp inputrc ~/.inputrc
	cp signature ~/.signature
